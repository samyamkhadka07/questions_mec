# Agent handoff

Start from the latest Git commit and read `WORK_CHECKPOINT.md` plus the phase matrix. Do not restart or create another migration lineage. The Workflow SDK advisory was resolved by replacing the SDK with PostgreSQL-backed leases/checkpoints and authenticated bounded Cron execution; the production dependency audit is clean.

Live Supabase verification has now proved authoritative scoring and symmetric two-user RLS isolation for profiles, entitlements, attempts, bookmarks, flashcards and contribution metadata. Migration 0015 repairs the production identity/function privilege drift found during that run. Migration 0016 is live and implements request-only Admin registration plus SUPER_ADMIN-only approval; the designated existing account was bootstrapped through trusted database administration without placing its email in source or documentation.

The current authoritative and deployed commit is `6fc2d7c23a1921cf64c428742c19c4a5c6edebff`, served by Vercel deployment `dpl_HgqoCdWsxmnokReNEX52VkNzfTLj`. Migration 0018 is live. The latest change separates `AppShell` and `AdminShell`, provides dedicated Admin modules, repairs the staged import/verify/publish lifecycle, exposes bounded document processing controls, and enforces Premium learning-game access in both the server API and PostgreSQL. Local tests total 51. Red-Chrome Production verification passed for the dedicated Admin shell, processing details/actions and the safe empty staged state.

The Ashar source is retained, but its 151 image pages are not yet academically imported. Continue from `docs/ASHAR_IMPORT_AUDIT.md`: pair OCR-corrected questions to the printed solutions and preserve page/set provenance; add the correct answer, explanation and option explanations; map taxonomy; then verify/publish reviewed rows only. Never bulk-publish corrupt OCR or infer missing answers.

For a new ChatGPT ID, connect the same GitHub account and red Chrome, clone `https://github.com/samyamkhadka07/myquiz-production`, verify the commit above, then read `docs/WORK_CHECKPOINT.md`, `docs/ASHAR_IMPORT_AUDIT.md`, `docs/SECURITY.md` and this handoff before acting. Reconnect Supabase project `pfrmuxkescvstqwwbgsv` and the existing Vercel project through secure browser authentication; do not paste credentials or tokens into chat. Do not create replacement projects or migration lineages. Repository `questions_mec` is a byte-identical secondary working copy, not the Vercel production source.

Remaining hard gates include the controlled pending/rejected/ordinary-ADMIN browser journeys, broader credential-backed browser CRUD, actual private Storage/TUS plus signed download, complete Ashar academic review/import, Premium student UI verification with a controlled entitled account, the packaged Playwright suite when Chromium is obtainable, and optional provider checks. Never promote local code or SQL/RLS evidence into browser, Storage or academic-publication evidence.
